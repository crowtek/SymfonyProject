<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Test Seite{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('css/styles.css') }}">
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        <div class="header">
            <div class="Logo">
                <span>TE</span>
                <span>ST</span>
            </div>
            <div class="header-links">
                <a href="{{ path('home')}}">HOME</a>
                <a href="{{ path('info')}}">INFORMATION</a>
                <a href="{{ path('apiPage')}}">API</a>
            </div>
        </div>
        {% block body %}
        {% endblock %}
        <div class="site-deko"></div>

        <div class="site-name">
            <div class="site-name-line"></div>
            <span>M</span>
            <span>E</span>
            <span>I</span>
            <span>K</span>
            <div class="site-name-line"></div>
        </div>
    </body>
</html>
